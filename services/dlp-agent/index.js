// Entry point for orchestrating the DLP agent lifecycle
function runDlpAgent(prompt) {
  // TODO: wire up claudeClient, rulesEngine, and responseMapper
  return { decision: "unknown", reason: "not implemented" };
}

module.exports = { runDlpAgent };
